# 프록시 대상 백엔드 주소 (서버 전용 환경변수, 브라우저로 노출되지 않음)
# next.config.ts 의 rewrites 가 `/api/*` 요청을 이 주소로 중계한다.
# 미설정 시 http://localhost:8080 (Spring Boot 기본 포트)
BACKEND_API_URL=http://localhost:8080

# msw 모킹 스위치. 개발 모드에서는 기본 활성화되며, 끄고 실제 백엔드를 붙이려면 disabled 로 둔다.
# NEXT_PUBLIC_API_MOCKING=disabled
